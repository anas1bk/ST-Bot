{
  "name": "engineering-telegram-bot",
  "version": "1.0.0",
  "description": "A clean, modular Telegram bot for engineering students",
  "main": "start.js",
  "scripts": {
    "start": "node start.js",
    "dev": "echo \"🚫 LOCAL RUNNING BLOCKED\" && echo \"🚫 This bot can only run on hosting services\" && echo \"🚫 Deploy to Render to use this bot\" && exit 1",
    "local": "echo \"🚫 LOCAL RUNNING BLOCKED\" && echo \"🚫 This bot can only run on hosting services\" && echo \"🚫 Deploy to Render to use this bot\" && exit 1",
    "run": "echo \"🚫 LOCAL RUNNING BLOCKED\" && echo \"🚫 This bot can only run on hosting services\" && echo \"🚫 Deploy to Render to use this bot\" && exit 1",
    "bot": "echo \"🚫 LOCAL RUNNING BLOCKED\" && echo \"🚫 This bot can only run on hosting services\" && echo \"🚫 Deploy to Render to use this bot\" && exit 1",
    "test": "echo \"🚫 LOCAL RUNNING BLOCKED\" && echo \"🚫 This bot can only run on hosting services\" && echo \"🚫 Deploy to Render to use this bot\" && exit 1"
  },
  "keywords": [
    "telegram",
    "bot",
    "engineering",
    "education",
    "nodejs"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "node-telegram-bot-api": "^0.65.0"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
